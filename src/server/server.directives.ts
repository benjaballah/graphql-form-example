import { RecrusiveDirective } from "../lib/form/directives/recursive.directives";
import { UpperCaseDirective } from "../lib/form/directives/uppercase.directives";

export const schemaDirectives = {
  recursive: RecrusiveDirective,
  upper: UpperCaseDirective
};
